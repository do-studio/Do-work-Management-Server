const monthlyTaskControllers = {
  addMonthlyTask: async (req, res) => {
    return res.status(200).json({
      status: true,
      message: "Placeholder for legacy addMonthlyTask route."
    });
  }
};

export default monthlyTaskControllers;
